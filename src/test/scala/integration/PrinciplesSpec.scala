package integration

import org.scalatest.FreeSpec

class PrinciplesSpec extends FreeSpec {
  "A rope always" - {
    "can be round-tripped through parsing and writing" in pending
    "can be round-tripped through writing and parsing" in pending
  }
}
